// Number is a primitive data type in TypeScript that represents numeric values.
// It includes both integer and floating-point values.

namespace NumberType {
  let intValue: number = 42;
  let floatValue: number = 3.14;
}
